# clasp
.clasp.json
.clasprc.json

# node
node_modules/
package-lock.json

# os
.DS_Store
Thumbs.db

# local scratch
*.local
/tmp/

# ผลลัพธ์จากการ build (สร้างใหม่ได้ด้วย node artifact/build.js)
dist/

# ไฟล์ระหว่างทางของ build/ — build/Code.gs กับ build/Index.html ต้องเข้า git
build/seed.json
build/db.b64
build/*.xlsx
