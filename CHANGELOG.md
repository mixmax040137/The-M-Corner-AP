# ประวัติการเปลี่ยนแปลง

## 1.4.0 — 2 ก.ย. 2026

ทั้งระบบนับปีเป็น **ค.ศ.** ให้ตรงกับปีที่เก็บในชีตและปฏิทินสากล

### สิ่งที่พบตอนตรวจ

- **ข้อมูลวันที่ในชีตเป็น ค.ศ. อยู่แล้ว** (`2026-01-19` · `year=2026`) ไม่มีอะไรต้องแปลง
  ที่เป็น พ.ศ. คือการแสดงผลบนหน้าจอ 7 จุดที่บวก 543 ตอนแสดง
- **ช่องตั้งค่า "รูปแบบปีที่แสดง" มีมาตั้งแต่แรกแต่ไม่มีโค้ดไหนอ่านค่าเลย**
  กดเปลี่ยนเป็น ค.ศ. แล้วไม่มีอะไรเกิดขึ้น — เป็นปุ่มหลอกมาตลอด
- **มีข้อมูลที่เป็น พ.ศ. จริงอยู่ 31 รายการ** ในช่อง `งวดที่` ของรายการโอนใช้หนี้
  ซึ่งเป็นช่องข้อความอิสระ (`7/2569` · `2565`)

### สิ่งที่แก้

- ทำให้ช่องตั้งค่า "รูปแบบปีที่แสดง" ใช้งานได้จริง ทั้งฝั่งหน้าเว็บและฝั่งเซิร์ฟเวอร์
  (วันที่ในตาราง · ช่องเลือกปี · ตารางแยกตามปี · อีเมลสรุป · ป้ายรอบบิล)
- เปลี่ยนค่าเริ่มต้นเป็น **ค.ศ.** และตัวย้ายรุ่น 8 ตั้งค่าให้เครื่องที่ติดตั้งไว้แล้วด้วย
  ใครอยากกลับไปใช้ พ.ศ. เลือกได้เองในหน้าตั้งค่า สลับได้ทันทีทั้งระบบ
- แปลงช่อง `งวดที่` 31 รายการจาก พ.ศ. เป็น ค.ศ. (`7/2569` → `7/2026`)
  ตัวแปลงผูกกับทั้งข้อความพอดี จึงไม่ไปแตะข้อความที่บังเอิญมีเลขคล้ายปี
  เช่นชื่อร้าน "ฟาฮาน่า แมทเทรส 2560 จำกัด" หรือรหัสรายการ `BUY-MTL6QOVE2531`
  — ตรวจแล้วยอดเงินทุกตัวไม่ขยับ จำนวนรายการเท่าเดิม

### สิ่งที่ไม่เปลี่ยน

การ *รับ* ค่าเข้ายังรองรับ พ.ศ. เหมือนเดิม — พิมพ์ `19/01/2565` หรือให้ OCR
อ่านใบเสร็จที่เป็น พ.ศ. ก็ยังได้ ระบบแปลงเป็น ค.ศ. ให้ตอนบันทึก

## 1.3.1 — 2 ก.ย. 2569

ตรวจทั้งระบบอีกรอบ เน้นของที่เพิ่งเพิ่ม (กราฟ · แถบล่างมือถือ · ตารางเป็นการ์ด)

### บั๊กที่แก้

- **ตัวเลขบนหน้าจอบอกผิดปีได้** — แต่ละหน้ามีช่วงปีไม่เท่ากัน (ซื้อของมีถึงปี 2563
  แต่รายรับ-รายจ่ายมีแค่ปีล่าสุด) พอสลับหน้า ระบบจะปรับปีให้ แต่ยังเอาข้อมูลของปีเก่า
  มาแสดงใต้ป้ายปีใหม่ ผลคือหน้าบอกว่า **"ยอดซื้อ ปี 2026 = 0 บาท"**
  ทั้งที่ปีนั้นซื้อจริง 28,676 บาท — ตอนนี้ปรับปีแล้วดึงข้อมูลใหม่ให้ตรงกัน
- **วันที่ที่พิมพ์ผิดถูกเก็บเป็นค่าว่างเงียบ ๆ** — แถวนั้นจะยังถูกนับในยอดรวมของปี
  แต่ไม่โผล่ในเดือนไหนเลย ยอดสรุปกับกราฟจึงไม่ตรงกันโดยไม่มีใครรู้
  ตอนนี้ปฏิเสธตั้งแต่ตอนบันทึกพร้อมบอกว่าผิดตรงไหน (วันที่ซื้อ · วันที่ชำระ · วันที่รายรับ-รายจ่าย)

### การทดสอบที่เพิ่ม

- เตรียมข้อมูลจำลองให้ครบทุกปีที่มีข้อมูลจริง จากเดิมมีแค่ 3 ปีล่าสุด
  ปีที่เหลือจึงไม่เคยถูกทดสอบเลย
- ทดสอบทุกปีในช่องเลือกปี × ทุกหน้า ว่าเปิดได้และป้ายตรงกับข้อมูล
- ทดสอบกราฟกับข้อมูลสุดขั้ว: ทุกเดือนเป็นศูนย์ · มีข้อมูลเดือนเดียว · ขาดทุนทุกเดือน ·
  ตัวเลข 8 หลัก · ทศนิยมจิ๋ว · ชื่อเดือนที่มีอักขระ HTML
- ทดสอบตัวติดป้ายคอลัมน์: ตารางซ้อนตาราง · เรียกซ้ำหลายรอบ · หัวตารางมี colspan
- กวาดทุกหน้า × 6 ปี × 2 ความกว้างจอ พร้อมเปิดทุกฟอร์มและทุกหน้าห้อง

### ที่ตรวจแล้วไม่มีปัญหา

ยอดเงินทุกตัวตรงกับผลบวกตรง ๆ · ทุกคำสั่งที่หน้าเว็บเรียกมีอยู่จริง ·
ไม่มีชื่อฟังก์ชันซ้ำทั้งฝั่งหน้าเว็บและเซิร์ฟเวอร์ · ค่าลับไม่หลุด ·
ลิงก์ดูอย่างเดียวสั่งอะไรไม่ได้ · รหัสผ่านเก็บเป็นแฮชมีเกลือ · PIN กันเดาได้ ·
กดบันทึกรัว ๆ ไม่เกิดรายการซ้ำ (ปุ่มถูกปิดทันที)

## 1.3.0 — 2 ก.ย. 2569

หน้าเว็บกับมือถือใช้พื้นที่จอต่างกันมาก รุ่นนี้จัดวางให้เหมาะกับแต่ละเครื่อง

**ทำเป็นโค้ดชุดเดียว ไม่ได้แยกเป็นสองแอป** — ไม่มีการเช็คว่าเป็นมือถือแล้วสลับไฟล์
หรือสลับเทมเพลต ข้อมูล ตรรกะ ปุ่ม ตารางทุกอย่างใช้ร่วมกันหมด
ต่างกันแค่ "วางตรงไหน" ซึ่งตัดสินด้วย CSS media query อย่างเดียว
ผลคือย่อ-ขยายหน้าต่างบนคอมก็สลับได้ทันทีโดยไม่ต้องโหลดหน้าใหม่
และเพิ่มฟีเจอร์ครั้งเดียวได้ทั้งสองแบบ ไม่ต้องแก้สองรอบ

### บนมือถือ (จอแคบกว่า 760px)

- **แถบเมนูล่าง** ระดับนิ้วโป้ง — ภาพรวม · รายรับ-จ่าย · ล้างแอร์ · ซ่อมห้อง · เพิ่มเติม
  ใช้รายการหน้าชุดเดียวกับเมนูข้าง เรียกฟังก์ชันเปลี่ยนหน้าตัวเดียวกัน
  มีตัวเลขงานค้างเหมือนกัน ส่วนหน้าที่ไม่ได้อยู่บนแถบ งานค้างจะไปรวมที่ปุ่ม "เพิ่มเติม"
  จะได้ไม่มีงานค้างไหนหายไปจากสายตา
- **ตารางกลายเป็นการ์ดทีละรายการ** พร้อมชื่อคอลัมน์กำกับทุกช่อง
  จากเดิมตารางกว้าง 1,020px ต้องเลื่อนซ้ายขวาตลอด ตอนนี้เหลือเท่าความกว้างจอ
  ช่องที่ไม่มีค่าไม่ต้องโชว์ การ์ดจะได้ไม่ยาวเก้อ
- **ตัวเลขสรุปเรียง 2 ช่องต่อแถว** จากเดิมช่องเดียวจนกินทั้งหน้าจอก่อนถึงเนื้อหา
- **ฟอร์มเต็มจอแบบแผ่นเลื่อนขึ้น** ปุ่มบันทึกติดขอบล่างเสมอ ไม่ต้องเลื่อนหา
- ปุ่มใหญ่พอให้นิ้วกด · ช่องกรอกใช้ฟอนต์ 16px เพื่อไม่ให้ iOS ซูมเองตอนแตะ
- เว้นระยะขอบล่างให้เครื่องที่มีแถบโฮม (iPhone)

### บนแท็บเล็ต / หน้าต่างย่อ

- แถบหัวบีบให้เหลือแถวเดียวตั้งแต่ 1040px ลงมา จากเดิมตกบรรทัดจนสูงเกือบเท่าตัว

### เบื้องหลัง

- ชื่อคอลัมน์ที่กำกับแต่ละช่องติดให้อัตโนมัติหลังวาดหน้าเสร็จ ที่เดียวจบ
  ทุกตารางในระบบจึงได้ผลเหมือนกันหมด ตารางที่เพิ่มใหม่ทีหลังก็ได้ไปด้วย
  ไม่ต้องไล่แก้ทีละตาราง
- เพิ่มการทดสอบ 25 ข้อที่ย่อหน้าต่างลงเป็นมือถือแล้วขยายกลับ
  ตรวจว่าสลับได้จริงและกลับมาเหมือนเดิม

## 1.2.0 — 2 ก.ย. 2569

### เพิ่มใหม่

- **กราฟกำไรขาดทุนรายเดือน** ในหน้ารายรับ-รายจ่ายหอ สองอัน
  - แท่งคู่เทียบรายรับกับรายจ่ายของแต่ละเดือน
  - แท่งกำไร/ขาดทุนสุทธิ ขึ้นจากเส้นศูนย์คือกำไร ลงคือขาดทุน
  - ชี้ที่เดือนไหนก็ได้ตัวเลขเดือนนั้น เลื่อนด้วยแป้นพิมพ์ก็ได้
  - ตัวเลขทุกตัวยังอ่านได้จากตารางข้างล่างเหมือนเดิม กราฟไม่ได้มาแทนตาราง

  **เรื่องสี** เดิมตั้งใจใช้เขียว = รายรับ แดง = รายจ่าย ตามที่คุ้นกัน
  แต่วัดแล้วคนตาบอดสีเขียว-แดงแยกสองสีนี้ไม่ออก (ค่าความต่าง 4.9 ต่ำกว่าเกณฑ์ 6)
  จึงเปลี่ยนเป็นน้ำเงิน = เงินเข้า ส้ม = เงินออก ซึ่งวัดได้ 27.6 ผ่านสบายทั้งสองโหมด
  และในกราฟกำไรขาดทุน ตำแหน่งเหนือ/ใต้เส้นศูนย์เป็นตัวบอกหลักอยู่แล้ว สีเป็นแค่ตัวย้ำ

## 1.1.0 — 2 ก.ย. 2569

รุ่นนี้เน้นตรวจทั้งระบบแล้วเก็บกวาดจุดที่ยังไม่ถูกต้อง พร้อมเติมส่วนที่ยังใช้งานไม่ได้จริง

### เพิ่มใหม่

- **เช็คลิสต์งานซ่อม** — เข้าห้องครั้งเดียวซ่อมได้หลายจุด แต่ละจุดเลือกประเภทงานของตัวเองได้
  หน้ารายการแสดงเป็น Todo List ติ๊กได้ทีละข้อ มีแถบความคืบหน้า ติ๊กแล้วบันทึกทันที
  สถานะงานขยับตามเอง (ติ๊กบางข้อ → กำลังซ่อม · ติ๊กครบ → เสร็จสิ้น)
- **ทะเบียนทรัพย์สินประจำห้อง เพิ่ม/แก้ไข/ลบได้จากหน้าเว็บ** — เดิมดูได้อย่างเดียว
  ต้องไปพิมพ์ในชีตเอง ทั้งที่ฝั่งเซิร์ฟเวอร์ทำไว้ครบแล้ว
- **ตัวเลขงานค้างหลังชื่อเมนู** เปลี่ยนจากนับใบงานเป็นนับ "จุด" ที่ยังไม่ได้ทำ

### แก้ไข

- **ห้องที่ยังไม่ได้ลงทะเบียนหายไปจากภาพรวม** — เพิ่มห้องใหม่ ปรับเลขห้อง หรือพิมพ์เลขห้องผิด
  งานนั้นยังถูกนับในยอดรวมแต่ไม่มีช่องห้องให้กดดู ตัวเลขจึงไม่ลงกัน
  กระทบทั้งหน้าซ่อมแซมตามห้อง ตารางล้างแอร์ และรายงานค่าใช้จ่ายรายห้อง
- **วันที่ที่ไม่มีอยู่จริงถูกเลื่อนเงียบ ๆ** — พิมพ์ 31/02 แล้วระบบบันทึกเป็น 3 มี.ค.
  ทำให้ยอดไปโผล่ผิดเดือนโดยไม่มีใครรู้ ตอนนี้ปฏิเสธวันที่ที่ไม่มีจริงแทน
- **ฟังก์ชันอ่านอีเมลผู้ใช้มีสองตัวที่คืนค่าต่างกัน** ตัวไหนทับตัวไหนขึ้นกับลำดับโหลดไฟล์
  ซึ่งเดาไม่ได้ และค่านี้ถูกใช้ในการตรวจสิทธิ์ — รวมเหลือตัวเดียวที่ "ไม่รู้ = ไม่ผ่าน"
- **กดบันทึกงานซ่อมแล้วพัง** เพราะมีโค้ดวาดหน้าจอหลุดไปปนอยู่ในตัวอ่านค่าจากฟอร์ม
- **หน้าซ่อมแซมตามห้องยังแสดงรายการเป็นข้อความเปล่า** เพราะอ่านข้อมูลคนละทางกับที่แนบเช็คลิสต์ไว้
- **ค่าสำรองของรอบตรวจข้อมูลเป็น 25 วินาที** ทั้งที่ค่าตั้งต้นจริงคือ 5 นาที
  ถ้าแถวตั้งค่าหาย ระบบจะกลับไปตรวจถี่เหมือนเดิมโดยไม่มีใครสั่ง
- แถวที่ชีตเดิมไม่ได้ระบุรายการซ่อม เคยเติมข้อความแทนไว้ กลายเป็นงานค้างหนึ่งข้อในเช็คลิสต์

### กันไม่ให้เกิดซ้ำ

- ตอน build จะไม่ผ่าน ถ้ามีชื่อฟังก์ชันซ้ำข้ามไฟล์ `.gs` (เดิมกันเฉพาะไฟล์ `ui/`)
- ตอน build จะไม่ผ่าน ถ้า `opt()` เรียกชื่อตัวเลือกที่ `app.bootstrap` ไม่ได้ส่งมา
- เพิ่มการทดสอบหน้ารายละเอียดห้อง ซึ่งเดิมไม่มีข้อมูลจำลองให้จึงไม่เคยถูกทดสอบเลย
- ชุดทดสอบรวม 432 + 41 + 176 + 27 ข้อ

## 1.0.0

รุ่นแรก — หนี้หลัก/หนี้รอง · รายการซื้อของ · ล้างแอร์ · ซ่อมแซมตามห้อง · ซ่อมแซมตึก ·
รายรับ-รายจ่ายหอ · ทะเบียนห้อง · รายงาน · สำรองข้อมูล · ระบบผู้ใช้และ PIN ·
อ่านข้อความจากรูป · หน้าตั้งค่าและธีมสว่าง/มืด
